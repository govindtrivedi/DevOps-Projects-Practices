name: CICD

on:
  workflow_dispatch:

  jobs:

  code:
     runs-on: ubuntu-latest
   steps:
      - name: checkout code
        step: echo "code clone from github"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build docker Container
        steps: echo "Docker Conatainer build"
      
  test:
    runs-on: ubuntu-latest
  steps:
    - name : Test Conatainer
      steps: echo "Testing the Container"
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy App
      steps: echo "docker compose delpy app"
